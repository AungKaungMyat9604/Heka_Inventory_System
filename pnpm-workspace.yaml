# pnpm 11+: dependency install scripts must be explicitly allowed (strictDepBuilds).
# Replaces removed onlyBuiltDependencies — see https://pnpm.io/settings#allowbuilds
allowBuilds:
  argon2: true
  core-js: true
  esbuild: true
